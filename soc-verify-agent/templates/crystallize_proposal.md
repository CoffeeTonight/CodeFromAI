# Crystallize proposal (LLM writes after successful PASS + promote approve)

script: {{script_name}}
stage: {{stage}}
group: {{group}}

## Rationale
(One paragraph: what logic is now stable enough to freeze as Python.)

## Python
```python
# Deterministic ops script — platform writes to ops/{stage}/{group}.py
```