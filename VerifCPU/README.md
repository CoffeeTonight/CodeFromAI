# VerifCPU (@ __CFA)

SSOT for SoC integration gates is **only**:

```
verif_cpu_verilog/
```

- `RTL_ROOT` = `~/tools/__CFA/VerifCPU/verif_cpu_verilog` (`discovered.yaml` `rtl_subdir`)
- Quick check: `./verif_cpu_verilog/example.sh` or `make -C verif_cpu_verilog soc-paste`
- Python reference model / legacy `verif_cpu_project` is **not** shipped here.
