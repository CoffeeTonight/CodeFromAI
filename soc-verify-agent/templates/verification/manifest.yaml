stage: simulation
group: example_group
milestone: M2          # plan별 id — soc-verify milestone plans 로 확인
status: active         # scheduled | active
schedule: tag_refresh  # tag_refresh | milestone | YYYY-MM-DD