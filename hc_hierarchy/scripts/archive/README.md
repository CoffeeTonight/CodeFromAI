# Archived verify scripts (phase 0–25)

일상 검증은 루트의 **`scripts/verify_v1.sh`** 만 사용하면 됩니다.

이 디렉터리의 `verify_phaseN.sh` 는 개발 이력용이며 CI/문서 기본 경로가 아닙니다.

```bash
# 예: 예전 phase14만 돌리기
bash scripts/archive/verify_phase14.sh
```