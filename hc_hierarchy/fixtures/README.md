# Fixtures

Golden DQL queries and small `.hch.db` snapshots will be added in Phase 3–4.

Phase 0–1 / top_module DQL: `../../design/unified_verify/` (`rtl/hfa/`, `--top top_module`).  
Phase 5+: `design/synthetic_deep_rtl/`, `design/extras/` (in-repo). See `design/README.md`.