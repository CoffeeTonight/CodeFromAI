# User-provided artifacts for tag `{TAG}` — update when docs arrive
tag: "{TAG}"
project_id: VERIF-CPU-SOC
updated_at: null
updated_by: null

artifacts:
  - path: deployment/customer_soc_intake.yaml
    kind: deployment
    rev: r0.1
    used_by: [static/coi_conn, simulation/slave_rw]
    note: "SoC integration intake — copy from customer_soc_intake.example.yaml and edit"
  - path: deployment/integration_notes.md
    kind: deployment
    rev: r0.1
    used_by: [static/coi_conn, simulation/slave_rw]
    note: "Human notes — NOT regenerated by example.sh gen"
  - path: deployment/questions_pending.md
    kind: deployment
    rev: r0.1
    used_by: [static/coi_conn, simulation/slave_rw]
    note: "Open questions for agent/user — NOT regenerated by gen"