# DQL Basic Examples
# One query per line. # comments and blank lines are ignored.
# This format exists so you never have to fight " and * escaping again.

module ~ "uart"
uart*
uart*5*
module ~ "uart" AND port ~ "irq"
NOT module = "testbench"
file ~ "*.v"
module in ("uart_16550", "spi_master")
port !~ "irq"
